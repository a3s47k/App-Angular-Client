.row
	.col
		.item(ng-repeat="item in data")
			h2
				a(ui-sref="details({id: item.id})") {{item.name}}
			p {{item.code}}
			p {{item.id}}
			p
				img(ng-src="http://localhost:7000{{item.img}}", alt="", srcset="")

